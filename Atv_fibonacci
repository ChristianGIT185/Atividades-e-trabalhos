#include <stdio.h>
int Funcao(int x, int y){
  if(y==0){
    return 1;
  } else{
    return x * Funcao(x, y-1);
  }
}
int funcFib(int n){
  if(){
    
  } else{
    
  }
}
int main(){
  double base, expoente;
  printf("Informe a base e o expoente:\n");
  scanf("%d %d", &base, &expoente);

  printf("%d elevado a %d é igual a %d", base, expoente, Funcao(base, expoente);
  return 0;
}
